{
  "name": "juice-web-tool",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -t rollupify index.js > index.build.js",
    "start": "http-server -d"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "http-server": "^0.9.0"
  },
  "devDependencies": {
    "juice": "^1.11.0",
    "browserify": "^13.0.1",
    "rollupify": "^0.3.0"
  }
}
